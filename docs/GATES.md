# Gate1-Gate5 验收检查清单

## Gate1：仓库结构与文档完整性（自动）

| 检查项 | 预期 | 状态 |
|---|---|---|
| skill.yaml 存在 | ✅ | 必含名称、版本、调用命令格式 |
| README.md 存在 | ✅ | 包含安装、使用示例、模块概览 |
| guidelines/ 目录存在 | ✅ | 10条核心规则文档 |
| modules/ 目录存在 | ✅ | 可执行实现模块 |
| assets/ACT0/ 目录存在 | ✅ | ACT0 场景规范与素材 |
| scripts/validate.js 存在 | ✅ | 规则校验 CLI |
| tests/ 目录存在 | ✅ | 测试脚本 |
| docs/ 目录存在 | ✅ | 设计说明文档 |

**运行：** `node scripts/validate.js --gate 1`

## Gate2：模块文件与规则完整性（自动+人工）

| 检查项 | 预期 | 状态 |
|---|---|---|
| guidelines/ 下规则文件数 | ≥10 | ✅ 10个 |
| 每个规则文档含"目的" | ✅ | |
| 每个规则文档含"可测指标" | ✅ | |
| 每个规则文档含"P0/P1条件" | ✅ | |
| 每个规则文档含"示例参考" | ✅ | |
| P0 规则数量 | ≥5 | ✅ 6个（空间/虚实/比例/色彩/禁忌/交互） |
| P1 规则数量 | ≥3 | ✅ 4个（材料/光影/动势/时间） |

**运行：** `node scripts/validate.js --gate 2`

## Gate3：规则算法与示例（自动）

| 检查项 | 预期 | 状态 |
|---|---|---|
| validate.js 可运行 | ✅ | 无语法错误 |
| 内置 ACT0 示例可校验 | ✅ | 输出评分和违规列表 |
| 色彩校验可检测高饱和 | ✅ | S>60% 触发 P0 |
| 禁忌校验可检测皇家符号 | ✅ | 非宫殿场景五爪龙触发 P0 |
| 比例校验可检测偏离 | ✅ | 偏离经典比例>10% 触发 P1 |
| 交互校验可检测延迟 | ✅ | 响应>500ms 触发 P0 |
| 校验输出 JSON 兼容 | ✅ | 可被下游工具解析 |

**运行：** `node scripts/validate.js --gate 3`

## Gate4：UI/动效集成验证（集成）

| 检查项 | 预期 | 验证方式 |
|---|---|---|
| ACT0 场景可渲染 | 云海+单门正常显示 | 浏览器预览/录屏 |
| 鼠标移动扰动云海 | 云层粒子在鼠标位置扰动 | 交互测试 |
| 点击触发生长 | 微光→裂隙→苔藓生长 | 交互测试 |
| 拖拽门旋转 | 门体Y轴旋转，松手回弹 | 交互测试 |
| 滚动下潜旋流 | 镜头下潜，金粉螺旋 | 交互测试 |
| 心门打开 | 滚动至极限触发 | 交互测试 |
| 材质参数正确 | 木/石/雾/金参数符合规范 | 代码审查 |
| 色彩符合规范 | 全部低饱和，五方正色范围 | validate.js 校验 |
| 无俗套元素 | 无皇家符号/纹样满铺/AI国风感 | validate.js + 人工 |
| 帧率 ≥30fps | 桌面端流畅 | 性能测试 |

**验收：** 录屏展示全部交互效果，validate.js 评分 ≥80

## Gate5：用户验收与审美评估（人工）

| 检查项 | 预期 | 评分 |
|---|---|---|
| 设计师评审 | 80%以上规则项满意 | /10 |
| 产品经理评审 | 场景符合 HEARTMIRROR Act 0 定位 | /10 |
| "为什么是中国的"测试 | 去掉红色/飞檐/毛笔字后仍东方 | /10 |
| 反俗套扫描 | 无国潮贴图感/古装影视感/仿古景区感/AI国风感 | /10 |
| 交互语义完整 | 鼠标/点击/拖拽/滚动全部有对应意象 | /10 |
| 性能达标 | 桌面≥30fps，移动端≥24fps | /10 |
| 移动端适配 | 触摸替代鼠标/滚轮 | /10 |

**通过标准：** 总分 ≥56/70（80%），无单项 <5分

---

## 快速验收命令

```bash
# Gate1: 结构检查
node scripts/validate.js --gate 1

# Gate2: 规则完整性
node scripts/validate.js --gate 2

# Gate3: 算法验证
node scripts/validate.js --gate 3

# 全量校验（使用设计JSON）
node scripts/validate.js --input design.json

# ACT0 内置示例校验
node scripts/validate.js --scene ACT0_gate
```
